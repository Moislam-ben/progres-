# HTML & CSS Login Page

This repository contains a simple login page built with HTML and CSS. After logging in, users are redirected to a next page (`home.html`).

## Usage
1. Clone the repository:
   ```bash
   git clone https://github.com/username/login-page.git
